{
  "name": "lancedb-docs-test",
  "version": "1.0.0",
  "description": "auto-generated tests from doc",
  "author": "dev@lancedb.com",
  "license": "Apache 2",
  "dependencies": {
    "apache-arrow": "file:../nodejs/node_modules/apache-arrow",
    "@lancedb/lancedb": "file:../nodejs"
  },
  "scripts": {
    "build": "cd ../nodejs && npm run build && cd ../docs && tsc -b",
    "test": "npm run build && ls dist/*.js | xargs -n 1 node"
  },
  "devDependencies": {
    "@types/node": "^20.11.8",
    "typescript": "^5.3.3"
  }
}
